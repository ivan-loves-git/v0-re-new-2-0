---
phase: 03-scoring-ui-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/repreneurs/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Legacy pencil icon is no longer visible in Rating card header"
    - "Large 'Questionnaire Details' section is no longer visible on profile page"
    - "Profile page displays WHO/WHEN scores but no legacy editing interface"
    - "QuestionnaireFormV2 still renders and functions correctly"
  artifacts:
    - path: "app/(dashboard)/repreneurs/[id]/page.tsx"
      provides: "Cleaned profile page without legacy scoring UI"
      contains: "QuestionnaireFormV2"
  key_links:
    - from: "app/(dashboard)/repreneurs/[id]/page.tsx"
      to: "components/repreneurs/questionnaire-form-v2.tsx"
      via: "import and render"
      pattern: "QuestionnaireFormV2"
---

<objective>
Remove legacy Tier 1 scoring UI elements to prepare for new WHO/WHEN inline editors.

Purpose: Create a clean slate for Phase 4 (WHO/WHEN inline editors) by removing the old pencil icon and redundant questionnaire display section.

Output: Profile page with scores visible but no legacy editing interface (interim state).
</objective>

<execution_context>
@/Users/ivanpaudice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ivanpaudice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-scoring-ui-cleanup/03-RESEARCH.md
@app/(dashboard)/repreneurs/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Tier1InlineEditor from Rating card</name>
  <files>app/(dashboard)/repreneurs/[id]/page.tsx</files>
  <action>
Remove the legacy Tier 1 scoring pencil icon:

1. Remove the import statement at line 29:
   ```tsx
   import { Tier1InlineEditor } from "@/components/repreneurs/tier1-inline-editor"
   ```

2. Remove the component usage at line 341:
   ```tsx
   <Tier1InlineEditor repreneur={repreneur as Repreneur} />
   ```

The Rating card header will now show: Star icon + "Rating" + Info tooltip + Recommendation badge + Flags
No editing capability for legacy Tier 1 questionnaire (this is intentional - v2 is the new path).

Note: Keep the component file `components/repreneurs/tier1-inline-editor.tsx` - it may be used elsewhere (intake v1). Only remove from profile page.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no TypeScript errors after import removal.
Grep for "Tier1InlineEditor" in page.tsx - should return no matches.
  </verify>
  <done>
Tier1InlineEditor import and usage removed from profile page.
No TypeScript errors.
Pencil icon no longer in Rating card header.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy Questionnaire Details section</name>
  <files>app/(dashboard)/repreneurs/[id]/page.tsx</files>
  <action>
Delete the entire "Questionnaire Details" section (lines 594-723 after Task 1 removal):

This section displays legacy Q1-Q17 questionnaire answers in a 3-column grid with:
- Section title "Questionnaire Details"
- 16 read-only fields (Employment Status, Prior M&A Experience, Years of Experience, etc.)
- One editable Source dropdown

Look for this block:
```tsx
{/* ROW 3: Questionnaire Details (no cards, blank bg, 3-col list) */}
<div className="pt-6 border-t">
  <h3 className="text-sm font-medium text-gray-500 mb-4">Questionnaire Details</h3>
  ...
</div>
```

Delete the entire `<div className="pt-6 border-t">` block including the closing `</div>`.

Note: Legacy data remains accessible via QuestionnaireFormV2's collapsible "Legacy Data Section".
Note: The Source dropdown was in this section - if needed, it can be added to QuestionnaireFormV2 later.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no TypeScript errors.
Grep for "Questionnaire Details" in page.tsx - should return no matches.
Page file should now end with the Activity/Notes/Offers row and final closing tags.
  </verify>
  <done>
"Questionnaire Details" section completely removed.
No TypeScript errors.
Profile page ends cleanly after the Activity/Notes/Offers row.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify page loads and QuestionnaireFormV2 works</name>
  <files>app/(dashboard)/repreneurs/[id]/page.tsx</files>
  <action>
Final verification that the profile page still works:

1. Run the development server: `npm run dev`
2. Check for any console errors or warnings
3. Verify the file structure is clean:
   - All imports still used (no unused import warnings)
   - Page exports correctly
   - No runtime errors

If there are unused imports after removal (e.g., if any icon was only used in removed sections), remove those imports too.

Expected final state:
- Rating card shows WHO/WHEN scores, Tier 2 dimensions, recommendation badge, flags
- No pencil icon in Rating card header
- QuestionnaireFormV2 card still present and functional
- No "Questionnaire Details" section at bottom
- Page ends with Activity/Notes/Offers row
  </action>
  <verify>
`npm run dev` starts without errors.
`curl -s http://localhost:3000/repreneurs/[any-valid-id] | grep -c "QuestionnaireFormV2"` returns 0 (component is client-side).
TypeScript compiles: `npx tsc --noEmit` passes.
  </verify>
  <done>
Profile page loads correctly.
QuestionnaireFormV2 still renders.
No console errors.
Build passes.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **TypeScript check:** `npx tsc --noEmit` passes
2. **Build check:** `npm run build` succeeds
3. **Removed elements confirmed:**
   - `grep -n "Tier1InlineEditor" app/\(dashboard\)/repreneurs/\[id\]/page.tsx` returns nothing
   - `grep -n "Questionnaire Details" app/\(dashboard\)/repreneurs/\[id\]/page.tsx` returns nothing
4. **Kept elements confirmed:**
   - `grep -n "QuestionnaireFormV2" app/\(dashboard\)/repreneurs/\[id\]/page.tsx` returns the import and usage
   - `grep -n "who_score\|when_score" app/\(dashboard\)/repreneurs/\[id\]/page.tsx` returns score display code
</verification>

<success_criteria>
- [ ] Tier1InlineEditor import removed from page.tsx
- [ ] Tier1InlineEditor usage removed from Rating card header
- [ ] Questionnaire Details section (lines 594-723) completely removed
- [ ] TypeScript compiles without errors
- [ ] Build succeeds
- [ ] QuestionnaireFormV2 still imported and rendered
- [ ] WHO/WHEN score display still present in Rating card
</success_criteria>

<output>
After completion, create `.planning/phases/03-scoring-ui-cleanup/03-01-SUMMARY.md`
</output>
