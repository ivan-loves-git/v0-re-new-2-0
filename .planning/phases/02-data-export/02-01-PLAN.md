# 02-01: Database Export Script

## Objective
Create a database export script that captures all tables with verification metadata for audit trail before Phase 9 cleanup.

## Tasks

1. Create `data/exports/.gitkeep` to ensure folder tracked in git
2. Create `scripts/export-database.ts` with:
   - Env loading pattern from existing scripts
   - Paginated fetch to avoid Supabase 1000-row limit
   - Export all 5 core tables (repreneurs, offers, repreneur_offers, notes, activities)
   - Include metadata with record counts
   - Write timestamped JSON to `data/exports/`
3. Run script to generate export
4. Verify JSON structure and record counts
5. Commit and push

## Files Modified
- `scripts/export-database.ts` (created)
- `data/exports/.gitkeep` (created)
- `data/exports/export-2026-01-26T18-34-44-292Z.json` (generated)

## Verification
- [x] Export script runs without errors
- [x] JSON file created with valid structure
- [x] Metadata includes correct record counts (118 repreneurs, 7 offers, 20 repreneur_offers, 32 notes, 35 activities)
- [x] File committed to git for audit trail

## Execution Summary
- **Duration:** ~5 minutes
- **Commit:** `5896fd8` - chore(02-01): create database export script and snapshot
- **Records exported:** 212 total across 5 tables
