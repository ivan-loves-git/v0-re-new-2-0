---
phase: 01-scope-lock-and-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/044_create_opportunities_foundation.sql
  - lib/types/opportunity.ts
  - lib/actions/opportunities.ts
autonomous: true
requirements: [OPP-01, OPP-02, OPP-04, OPP-05, MNA-01]
user_setup: []
must_haves:
  truths:
    - Opportunity records have staff-only and repreneur-visible field boundaries represented in code.
    - Opportunity schema supports manual CRUD, Excel import, matching, documents, and future pursuit workflow.
    - M&A source/contact support is minimal and does not implement full CRM scope.
  artifacts:
    - scripts/044_create_opportunities_foundation.sql
    - lib/types/opportunity.ts
    - lib/actions/opportunities.ts
  key_links:
    - lib/actions/opportunities.ts uses createAdminClient and requireUser like existing staff actions.
    - lib/types/opportunity.ts mirrors SQL enums and row shapes.
---

<objective>
Create the Phase 1 data foundation for opportunities.

Purpose: This establishes the schema and server-side contract that every later Phase 1 task depends on.
Output: SQL migration, TypeScript opportunity types, and server actions for basic staff CRUD.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scope-lock-and-data-foundation/01-CONTEXT.md
@lib/actions/repreneurs.ts
@lib/types/repreneur.ts
@lib/supabase/admin.ts
@scripts/003_create_repreneur_offers_table.sql
@scripts/017_setup_cv_storage.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opportunity foundation SQL</name>
  <files>scripts/044_create_opportunities_foundation.sql</files>
  <read_first>scripts/003_create_repreneur_offers_table.sql, scripts/017_setup_cv_storage.sql, .planning/phases/01-scope-lock-and-data-foundation/01-CONTEXT.md</read_first>
  <action>Create a SQL migration for the Phase 1 foundation. Include tables for `ma_sources`, `opportunities`, and `opportunity_documents` only if document metadata is needed for the foundation. Use minimal M&A source/contact fields, not full CRM. Include staff-only fields and repreneur-visible/anonymized fields explicitly. Add indexes for opportunity status, date added, sector, location, and source. Include comments documenting that automatic PDF parsing and full CRM are out of scope.</action>
  <verify>rg -n "CREATE TABLE IF NOT EXISTS public.opportunities|CREATE TABLE IF NOT EXISTS public.ma_sources|staff_only|repreneur" scripts/044_create_opportunities_foundation.sql</verify>
  <acceptance_criteria>
    - SQL contains `public.opportunities`.
    - SQL contains staff-only source fields or a relation to `public.ma_sources`.
    - SQL contains a clear field or convention for repreneur-visible/anonymized data.
    - SQL does not create a full CRM activity/history model for M&A firms.
  </acceptance_criteria>
  <done>Opportunity foundation migration exists and encodes the Phase 1 boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add opportunity TypeScript types</name>
  <files>lib/types/opportunity.ts</files>
  <read_first>lib/types/repreneur.ts, scripts/044_create_opportunities_foundation.sql</read_first>
  <action>Create TypeScript types for opportunity rows, inserts, updates, document metadata, visibility, opportunity status, and minimal M&A source/contact. Match enum/string unions to the SQL values. Include helper constants for status and document visibility options.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - `lib/types/opportunity.ts` exports row/insert/update types.
    - Types include staff-only vs repreneur-visible visibility concepts.
    - TypeScript check passes or existing unrelated project errors are explicitly recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Opportunity types compile and are ready for server actions/UI.</done>
</task>

<task type="auto">
  <name>Task 3: Add basic opportunity server actions</name>
  <files>lib/actions/opportunities.ts</files>
  <read_first>lib/actions/repreneurs.ts, lib/supabase/admin.ts, lib/types/opportunity.ts</read_first>
  <action>Create staff-only server actions for list/get/create/update/archive opportunity. Use `createAdminClient()`, `requireUser()`, validation, and `revalidatePath()` following `lib/actions/repreneurs.ts`. Do not add repreneur-facing actions in this plan. Keep import/document operations as placeholders or separate exported helpers only if needed by later plans.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - Actions exist for list, get, create, update, and archive/unavailable.
    - Actions require authenticated staff user.
    - Actions revalidate `/opportunities` and relevant detail routes.
    - No repreneur-facing exposure action is included.
  </acceptance_criteria>
  <done>Staff opportunity data access is available for UI and import plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit --pretty false`
- [ ] `rg -n "opportunities|ma_sources|Opportunity" scripts/044_create_opportunities_foundation.sql lib/types/opportunity.ts lib/actions/opportunities.ts`
- [ ] Verify no V3 features are introduced.
</verification>

<success_criteria>

- All tasks completed
- The schema and actions support Phase 1 without V3 scope creep
- TypeScript check passes or unrelated existing failures are documented

</success_criteria>

<output>
After completion, create `.planning/phases/01-scope-lock-and-data-foundation/01-01-SUMMARY.md`
</output>
