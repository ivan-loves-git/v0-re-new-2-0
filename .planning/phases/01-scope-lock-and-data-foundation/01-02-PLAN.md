---
phase: 01-scope-lock-and-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/(dashboard)/opportunities/page.tsx
  - app/(dashboard)/opportunities/new/page.tsx
  - app/(dashboard)/opportunities/[id]/page.tsx
  - components/opportunities/opportunity-table.tsx
  - components/opportunities/opportunity-form.tsx
  - components/opportunities/opportunity-detail.tsx
  - components/app-sidebar.tsx
autonomous: true
requirements: [OPP-01, OPP-02, OPP-04]
user_setup: []
must_haves:
  truths:
    - Staff can list, view, create, edit, and archive opportunities.
    - Staff-only and repreneur-visible data are visually separated in the detail view.
    - Opportunity UI uses shadcn components and the existing dashboard style.
  artifacts:
    - app/(dashboard)/opportunities/page.tsx
    - components/opportunities/opportunity-table.tsx
    - components/opportunities/opportunity-form.tsx
    - components/opportunities/opportunity-detail.tsx
  key_links:
    - Opportunity pages call lib/actions/opportunities.ts from Plan 01.
    - Sidebar exposes the staff opportunity surface without exposing repreneur-facing routes.
---

<objective>
Build staff opportunity management screens.

Purpose: Staff need a usable internal surface for opportunity records before import, documents, matching, or repreneur exposure matter.
Output: Staff list/detail/create/edit screens using existing shadcn UI patterns.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scope-lock-and-data-foundation/01-CONTEXT.md
@lib/actions/opportunities.ts
@lib/types/opportunity.ts
@components/app-sidebar.tsx
@app/(dashboard)/analytics/page.tsx
@components/analytics/operational-kpis.tsx
@components/ui/card.tsx
@components/ui/table.tsx
@components/ui/badge.tsx
@components/ui/button.tsx
@components/ui/input.tsx
@components/ui/select.tsx
@components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add staff opportunities route and sidebar entry</name>
  <files>app/(dashboard)/opportunities/page.tsx, components/app-sidebar.tsx</files>
  <read_first>components/app-sidebar.tsx, app/(dashboard)/analytics/page.tsx</read_first>
  <action>Create `/opportunities` as a staff dashboard route. Add one sidebar item labelled `Opportunities` with a Lucide icon. Use the existing sidebar data structure and active-route logic. The page should fetch opportunities through `listOpportunities` and render a staff-oriented list section.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - `/opportunities` route exists.
    - Sidebar contains `Opportunities`.
    - Route uses server-side data fetching where possible.
  </acceptance_criteria>
  <done>Staff can navigate to the opportunities area.</done>
</task>

<task type="auto">
  <name>Task 2: Build opportunity table with shadcn Table</name>
  <files>components/opportunities/opportunity-table.tsx</files>
  <read_first>components/ui/table.tsx, components/ui/badge.tsx, components/ui/dropdown-menu.tsx, .planning/phases/01-scope-lock-and-data-foundation/01-CONTEXT.md</read_first>
  <action>Build an opportunity table using shadcn `Table`, `Badge`, `Button`, and `DropdownMenu`. Columns: reference, sector/activity, location, revenue, EBITDA, headcount, month/date added, status, source visibility. Include empty state using existing available shadcn primitives; if `Empty` is not installed, use `Card` plus muted copy rather than custom decorative UI. Do not use custom colored spans for statuses when `Badge` variants are enough.</action>
  <verify>rg -n "Table|TableHeader|TableRow|Badge|DropdownMenu" components/opportunities/opportunity-table.tsx</verify>
  <acceptance_criteria>
    - Table uses shadcn table primitives.
    - Status/source visibility use Badge.
    - No repreneur-only route or action is introduced.
  </acceptance_criteria>
  <done>Opportunity list is scannable and consistent with the app UI.</done>
</task>

<task type="auto">
  <name>Task 3: Build create/edit form and detail screen</name>
  <files>app/(dashboard)/opportunities/new/page.tsx, app/(dashboard)/opportunities/[id]/page.tsx, components/opportunities/opportunity-form.tsx, components/opportunities/opportunity-detail.tsx</files>
  <read_first>components/repreneurs/repreneur-form.tsx, components/ui/card.tsx, components/ui/input.tsx, components/ui/select.tsx, components/ui/tabs.tsx, lib/actions/opportunities.ts</read_first>
  <action>Build a staff create/edit form and detail view. Use shadcn `Card`, `Input`, `Textarea`, `Select`, `Button`, and `Tabs` where useful. Detail view must separate staff-only source data from repreneur-visible/anonymized data. Use server actions from Plan 01. Keep document upload, Excel import, and repreneur actions out of this plan.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - Staff can create an opportunity.
    - Staff can edit an opportunity.
    - Detail view has explicit staff-only and repreneur-visible sections.
    - UI uses shadcn components from `@/components/ui`.
  </acceptance_criteria>
  <done>Staff CRUD works for manually entered opportunities.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit --pretty false`
- [ ] `npm run lint`
- [ ] Browser-test `/opportunities`, `/opportunities/new`, and one detail route if local data is available.
</verification>

<success_criteria>

- Staff CRUD screens exist
- shadcn components are used for feature UI
- Staff-only/repreneur-visible separation is visible in UI

</success_criteria>

<output>
After completion, create `.planning/phases/01-scope-lock-and-data-foundation/01-02-SUMMARY.md`
</output>
