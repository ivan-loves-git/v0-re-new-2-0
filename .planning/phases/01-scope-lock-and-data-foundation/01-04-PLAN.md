---
phase: 01-scope-lock-and-data-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02"]
files_modified:
  - components/opportunities/ma-source-panel.tsx
  - components/opportunities/opportunity-form.tsx
  - components/opportunities/opportunity-detail.tsx
  - lib/actions/opportunities.ts
autonomous: true
requirements: [MNA-01, OPP-04]
user_setup: []
must_haves:
  truths:
    - Staff can store and view the basic M&A source/contact for an opportunity.
    - Source/contact data is staff-only by default.
    - The implementation does not become a full M&A CRM.
  artifacts:
    - components/opportunities/ma-source-panel.tsx
  key_links:
    - M&A source fields connect to the opportunity detail and form from Plan 02.
---

<objective>
Add minimal M&A source/contact handling.

Purpose: Opportunities need source/contact context for staff operations, but June must avoid building the full M&A CRM.
Output: Staff-only source/contact panel and form wiring.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scope-lock-and-data-foundation/01-CONTEXT.md
@lib/actions/opportunities.ts
@components/opportunities/opportunity-form.tsx
@components/opportunities/opportunity-detail.tsx
@components/ui/card.tsx
@components/ui/badge.tsx
@components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build staff-only M&A source panel</name>
  <files>components/opportunities/ma-source-panel.tsx</files>
  <read_first>components/ui/card.tsx, components/ui/badge.tsx, lib/types/opportunity.ts</read_first>
  <action>Create a compact staff-only panel showing firm/source name, contact name/email if available, and visibility state. Use shadcn `Card` and `Badge`. Include explicit copy or label that source details are staff-only. Avoid relationship history, CRM notes, outreach automation, or firm portal concepts.</action>
  <verify>rg -n "staff-only|Card|Badge|M&A|source" components/opportunities/ma-source-panel.tsx</verify>
  <acceptance_criteria>
    - Panel exists.
    - Panel uses shadcn primitives.
    - Panel does not include CRM features.
  </acceptance_criteria>
  <done>Staff-only source/contact display exists.</done>
</task>

<task type="auto">
  <name>Task 2: Wire source/contact into form and detail</name>
  <files>components/opportunities/opportunity-form.tsx, components/opportunities/opportunity-detail.tsx, lib/actions/opportunities.ts</files>
  <read_first>components/opportunities/ma-source-panel.tsx, lib/actions/opportunities.ts</read_first>
  <action>Add minimal source/contact fields to create/edit and display them only in the staff-only area of the detail view. Ensure update actions persist these fields. Do not expose these fields to any repreneur-visible data object or anonymized section.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - Create/edit captures source/contact fields.
    - Detail shows source/contact only in staff-only section.
    - Repreneur-visible section does not include source/contact fields.
  </acceptance_criteria>
  <done>Minimal source/contact is operational and bounded.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit --pretty false`
- [ ] `rg -n "source|ma" components/opportunities lib/actions/opportunities.ts`
- [ ] Manually inspect detail UI separation.
</verification>

<success_criteria>

- Staff can store source/contact
- Source/contact remains staff-only
- Full CRM scope remains excluded

</success_criteria>

<output>
After completion, create `.planning/phases/01-scope-lock-and-data-foundation/01-04-SUMMARY.md`
</output>
