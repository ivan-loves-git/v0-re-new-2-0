---
phase: 01-scope-lock-and-data-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - lib/utils/opportunity-import.ts
  - lib/actions/opportunity-import.ts
  - app/(dashboard)/opportunities/import/page.tsx
  - components/opportunities/opportunity-import-review.tsx
  - components/opportunities/opportunity-import-summary.tsx
autonomous: true
requirements: [OPP-03, OPP-04]
user_setup:
  - Real Excel attachment files from Bertrand's Notion may need to be copied into a local test fixture path before full manual validation.
must_haves:
  truths:
    - Staff can review imported rows before saving.
    - Missing required fields block only affected rows.
    - Missing optional fields produce warnings, not silent data loss.
    - Import does not parse PDFs or infer data from teasers.
  artifacts:
    - lib/utils/opportunity-import.ts
    - lib/actions/opportunity-import.ts
    - app/(dashboard)/opportunities/import/page.tsx
  key_links:
    - Import save path calls opportunity actions or Supabase schema from Plan 01.
    - Import review UI uses shadcn table/card/button patterns.
---

<objective>
Build Excel import mapping and review flow.

Purpose: Bertrand's Excel files are the real source for the first opportunity dataset, but staff must review mapped data before writing it.
Output: Excel parser/mapping utility, import action, import review page, and import summary UI.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scope-lock-and-data-foundation/01-CONTEXT.md
@lib/actions/opportunities.ts
@lib/types/opportunity.ts
@scripts/import-flatchr-excel.py
@components/ui/table.tsx
@components/ui/card.tsx
@components/ui/alert.tsx
@components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Excel import mapper</name>
  <files>lib/utils/opportunity-import.ts</files>
  <read_first>lib/types/opportunity.ts, .planning/phases/01-scope-lock-and-data-foundation/01-CONTEXT.md</read_first>
  <action>Create a mapper that accepts rows from Bertrand's source/shared Excel exports and returns normalized opportunity drafts plus row-level diagnostics. Include required-field blockers and optional-field warnings. Do not use string-only ad hoc parsing when a structured parser is available in the project dependencies. Map known fields: reference, source, location, sector/activity, description, revenue, EBITDA, headcount, date added.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - Mapper returns normalized drafts.
    - Mapper returns warnings and blockers per row.
    - Mapper has deterministic field mapping constants.
  </acceptance_criteria>
  <done>Excel rows can be transformed into reviewable opportunity drafts.</done>
</task>

<task type="auto">
  <name>Task 2: Add import server action</name>
  <files>lib/actions/opportunity-import.ts</files>
  <read_first>lib/actions/opportunities.ts, lib/actions/repreneurs.ts, lib/utils/opportunity-import.ts</read_first>
  <action>Create server actions to preview import and commit approved rows. Require user authentication. Commit should insert only approved valid rows and produce a summary with created, skipped, warning, and blocked counts. Revalidate `/opportunities` after commit.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - Preview action does not write rows.
    - Commit action writes approved valid rows only.
    - Summary counts are returned.
  </acceptance_criteria>
  <done>Import can be previewed and committed safely.</done>
</task>

<task type="auto">
  <name>Task 3: Add import review UI</name>
  <files>app/(dashboard)/opportunities/import/page.tsx, components/opportunities/opportunity-import-review.tsx, components/opportunities/opportunity-import-summary.tsx</files>
  <read_first>components/ui/table.tsx, components/ui/card.tsx, components/ui/alert.tsx, components/ui/badge.tsx, components/ui/button.tsx</read_first>
  <action>Build the import page with shadcn `Card`, `Table`, `Alert`, `Badge`, and `Button`. Staff should upload/select an Excel file, preview mapped rows, see warnings/blockers, and commit approved rows. If actual file upload is too large for the current action pattern, implement the review component and clearly document the remaining wiring in the SUMMARY.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - Import UI uses shadcn primitives.
    - Row diagnostics are visible before commit.
    - Staff can distinguish created/skipped/warning/blocked rows.
  </acceptance_criteria>
  <done>Staff has a safe review-first import surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit --pretty false`
- [ ] `npm run lint`
- [ ] Test mapper with a small fixture that includes valid, warning, and blocked rows.
</verification>

<success_criteria>

- Import is review-first, not silent
- Diagnostics prevent hidden data loss
- No PDF parsing is introduced

</success_criteria>

<output>
After completion, create `.planning/phases/01-scope-lock-and-data-foundation/01-03-SUMMARY.md`
</output>
