---
phase: 01-scope-lock-and-data-foundation
plan: 05
type: execute
wave: 5
depends_on: ["01-01", "01-02"]
files_modified:
  - scripts/045_setup_opportunity_documents_storage.sql
  - lib/actions/opportunity-documents.ts
  - components/opportunities/opportunity-documents-panel.tsx
  - components/opportunities/opportunity-detail.tsx
autonomous: true
requirements: [OPP-05]
user_setup:
  - Supabase storage bucket setup may require applying SQL or storage policy manually if not automated by the local workflow.
must_haves:
  truths:
    - Staff can attach documents to opportunities.
    - Documents are staff-only by default.
    - Repreneur visibility is explicit metadata, not implicit storage path behavior.
    - No inline PDF viewer or AI analysis is introduced.
  artifacts:
    - scripts/045_setup_opportunity_documents_storage.sql
    - lib/actions/opportunity-documents.ts
    - components/opportunities/opportunity-documents-panel.tsx
  key_links:
    - Document metadata links to opportunities from Plan 01.
    - Document panel appears in opportunity detail from Plan 02.
---

<objective>
Add opportunity document attachment storage and staff management.

Purpose: V2 needs teasers, deal books, NDA files, and external analysis PDFs linked to opportunities without building parsing or inline review.
Output: Storage setup, staff document actions, and opportunity document panel.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scope-lock-and-data-foundation/01-CONTEXT.md
@scripts/017_setup_cv_storage.sql
@lib/actions/opportunities.ts
@components/opportunities/opportunity-detail.tsx
@components/ui/card.tsx
@components/ui/button.tsx
@components/ui/badge.tsx
@components/ui/dropdown-menu.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add storage setup for opportunity documents</name>
  <files>scripts/045_setup_opportunity_documents_storage.sql</files>
  <read_first>scripts/017_setup_cv_storage.sql, scripts/044_create_opportunities_foundation.sql</read_first>
  <action>Create SQL/storage setup for opportunity document storage following existing CV/LDC storage conventions. Include policy comments and metadata expectations. Staff-only should be default. Do not implement public unauthenticated document access.</action>
  <verify>rg -n "opportunity|documents|bucket|storage|policy" scripts/045_setup_opportunity_documents_storage.sql</verify>
  <acceptance_criteria>
    - Storage setup references opportunity documents.
    - Default access is not public.
    - Setup documents staff-only default.
  </acceptance_criteria>
  <done>Storage setup exists for opportunity documents.</done>
</task>

<task type="auto">
  <name>Task 2: Add document server actions</name>
  <files>lib/actions/opportunity-documents.ts</files>
  <read_first>lib/actions/repreneurs.ts, lib/actions/opportunities.ts, lib/supabase/admin.ts</read_first>
  <action>Create server actions to list, upload/register, update visibility, and delete/remove opportunity documents. Require authenticated staff. Store metadata including title, document type/category, visibility, opportunity id, and uploaded timestamp. Keep repreneur download action out of Phase 1 unless needed for internal preview.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - Staff actions exist for list/register/update visibility/remove.
    - Default visibility is staff-only.
    - Actions revalidate opportunity detail route.
  </acceptance_criteria>
  <done>Staff document actions are available.</done>
</task>

<task type="auto">
  <name>Task 3: Add document panel to opportunity detail</name>
  <files>components/opportunities/opportunity-documents-panel.tsx, components/opportunities/opportunity-detail.tsx</files>
  <read_first>components/ui/card.tsx, components/ui/button.tsx, components/ui/badge.tsx, components/ui/dropdown-menu.tsx, lib/actions/opportunity-documents.ts</read_first>
  <action>Build a staff document panel using shadcn `Card`, `Button`, `Badge`, and `DropdownMenu`. Show title, type, visibility, uploaded date, and actions. Integrate it into the opportunity detail. Keep inline PDF viewer and AI analysis out of scope.</action>
  <verify>npx tsc --noEmit --pretty false</verify>
  <acceptance_criteria>
    - Detail page shows linked documents.
    - Visibility is visible and editable by staff.
    - UI uses shadcn primitives.
    - No inline PDF viewer exists.
  </acceptance_criteria>
  <done>Staff can manage opportunity document attachments.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit --pretty false`
- [ ] `npm run lint`
- [ ] Verify staff-only default for documents.
</verification>

<success_criteria>

- Staff document attachment foundation exists
- Visibility is explicit
- Deferred PDF viewer/AI analysis remain excluded

</success_criteria>

<output>
After completion, create `.planning/phases/01-scope-lock-and-data-foundation/01-05-SUMMARY.md`
</output>
