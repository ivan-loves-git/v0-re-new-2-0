---
phase: 01.1-testing-environment-and-release-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TESTING_RELEASE_PROTOCOL.md
  - .env.test.local.example
autonomous: false
requirements: [ENV-01, ENV-02, TEST-01, REL-01, REL-02]
user_setup:
  - Ivan approved using the same/current Supabase project on 2026-05-17.
must_haves:
  truths:
    - Phase 2 remains blocked until this protocol is usable.
    - Worktree isolation does not equal database isolation.
    - Migrations are allowed against the current Supabase project only because Ivan explicitly approved it.
    - Phase 1.1 allows additive migrations only.
    - No destructive SQL is allowed.
  artifacts:
    - docs/TESTING_RELEASE_PROTOCOL.md
    - .env.test.local.example
---

<objective>
Lock the Phase 1.1 environment policy around the same/current Supabase project with controlled migration rules.

Purpose: Avoid extra Supabase project cost while making database risk explicit before testing.
Output: Approved environment policy, local env template, and clear guardrails for same-project testing.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Record same-project Supabase as the Phase 1.1 target</name>
  <action>Update docs/TESTING_RELEASE_PROTOCOL.md and this phase context so the same/current Supabase project is treated as approved for controlled Phase 1.1 testing.</action>
  <acceptance_criteria>
    - Protocol says same/current Supabase project is selected.
    - Protocol says this is not an isolated sandbox.
    - Protocol says additive migrations only.
    - Phase 2 remains blocked until test environment and UAT pass.
  </acceptance_criteria>
  <done>Environment policy is locked.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare approved project configuration template</name>
  <action>Keep .env.test.local.example focused on the approved current Supabase project. When credentials are available, prepare the worktree .env.local without committing secrets.</action>
  <acceptance_criteria>
    - Required Supabase and Better Auth variables are documented.
    - Template warns that this is a controlled same-project migration, not a sandbox.
    - Secrets are not committed.
    - The worktree uses port 3011 for Phase 1 UAT.
  </acceptance_criteria>
  <done>Worktree can be configured for the approved Supabase project once credentials exist.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm no committed secrets</name>
  <action>Check repository state before committing this plan and again after any .env.local setup.</action>
  <acceptance_criteria>
    - No .env.local file is committed.
    - No Supabase service role key appears in tracked files.
    - The plan can be shared without exposing secrets.
  </acceptance_criteria>
  <done>Same-project plan is safe to commit.</done>
</task>

</tasks>

<verification>
- [ ] `git status --short` shows no committed secrets.
- [ ] docs/TESTING_RELEASE_PROTOCOL.md says same/current Supabase project is selected.
- [ ] .env.test.local.example warns this is not an isolated sandbox.
- [ ] Phase 2 remains blocked in the roadmap.
</verification>

<success_criteria>
- Environment choice is no longer open.
- Phase 1.1 has a concrete same-project migration path.
- Real/shared database risk is explicit and controlled.
</success_criteria>
