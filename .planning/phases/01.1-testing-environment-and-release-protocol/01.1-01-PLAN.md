---
phase: 01.1-testing-environment-and-release-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TESTING_RELEASE_PROTOCOL.md
  - .env.test.local.example
autonomous: false
requirements: [ENV-01, ENV-02, TEST-01, REL-01, REL-02]
user_setup:
  - Ivan must provide or approve access to a separate Supabase test project.
must_haves:
  truths:
    - Phase 2 remains blocked until this protocol is usable.
    - Worktree isolation does not equal database isolation.
    - Migrations are only applied to the separate Supabase test project during Phase 1.1.
    - The current/shared Supabase database is not touched during Phase 1.1.
  artifacts:
    - docs/TESTING_RELEASE_PROTOCOL.md
    - .env.test.local.example
---

<objective>
Lock the Phase 1.1 environment policy around Option 1: a separate Supabase test project.

Purpose: Make it impossible to accidentally validate Phase 1 against the current/shared Supabase database.
Output: Approved environment policy, local env template, and clear ownership of test credentials.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Record separate Supabase as the only Phase 1.1 target</name>
  <action>Update docs/TESTING_RELEASE_PROTOCOL.md and this phase context so Option 1 is treated as selected, not merely recommended.</action>
  <acceptance_criteria>
    - Protocol says separate Supabase test project is selected.
    - Protocol says current/shared Supabase is not touched in Phase 1.1.
    - Phase 2 remains blocked until test environment and UAT pass.
  </acceptance_criteria>
  <done>Environment policy is locked.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare test project configuration template</name>
  <action>Keep .env.test.local.example focused on the separate Supabase test project. When credentials are available, prepare the worktree .env.local without committing secrets.</action>
  <acceptance_criteria>
    - Required Supabase and Better Auth variables are documented.
    - Template warns not to use current/shared Supabase credentials.
    - Secrets are not committed.
    - The worktree uses port 3011 for Phase 1 UAT.
  </acceptance_criteria>
  <done>Worktree can be configured for the test Supabase project once credentials exist.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm no committed secrets</name>
  <action>Check repository state before committing this plan and again after any .env.local setup.</action>
  <acceptance_criteria>
    - No .env.local file is committed.
    - No Supabase service role key appears in tracked files.
    - The plan can be shared without exposing secrets.
  </acceptance_criteria>
  <done>Option 1 plan is safe to commit.</done>
</task>

</tasks>

<verification>
- [ ] `git status --short` shows no committed secrets.
- [ ] docs/TESTING_RELEASE_PROTOCOL.md says Option 1 is selected.
- [ ] .env.test.local.example warns against current/shared Supabase.
- [ ] Phase 2 remains blocked in the roadmap.
</verification>

<success_criteria>
- Environment choice is no longer open.
- Phase 1.1 has a concrete test Supabase setup path.
- No real/shared database can be touched by ambiguity.
</success_criteria>
