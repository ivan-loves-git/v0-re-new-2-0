---
phase: 01.1-testing-environment-and-release-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/TESTING_RELEASE_PROTOCOL.md
  - docs/PHASE1_UAT_RESULTS.md
  - .env.test.local.example
autonomous: false
requirements: [ENV-01, ENV-02, TEST-01, REL-01, REL-02]
user_setup:
  - Ivan must choose the database/environment option before migrations are applied.
must_haves:
  truths:
    - Phase 2 remains blocked until this protocol is usable.
    - Worktree isolation does not equal database isolation.
    - Migrations are only applied to an explicitly approved database.
  artifacts:
    - docs/TESTING_RELEASE_PROTOCOL.md
    - docs/PHASE1_UAT_RESULTS.md
    - .env.test.local.example
---

<objective>
Create the test environment and release protocol gate for Re-New Platform V2.

Purpose: Make database testing, UAT, push/PR/merge, and rollback decisions explicit before continuing Phase 2.
Output: Approved environment policy, UAT result log, and release protocol.
</objective>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: Choose testing database strategy</name>
  <action>Present Ivan with three options: separate test Supabase, current Supabase with manual backup, or local UI-only. Recommend separate test Supabase. Do not apply migrations until Ivan selects an option.</action>
  <acceptance_criteria>
    - Decision is recorded in docs/TESTING_RELEASE_PROTOCOL.md.
    - Rollback/backup expectation is explicit.
  </acceptance_criteria>
  <done>Testing database strategy is chosen and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare worktree test configuration</name>
  <action>Create `.env.test.local.example` documenting required variables. If Ivan chooses an environment and provides/approves credentials, prepare the worktree `.env.local` for that environment without committing secrets.</action>
  <acceptance_criteria>
    - Required environment variables are documented.
    - Secrets are not committed.
    - The worktree can be started on a non-default port.
  </acceptance_criteria>
  <done>Worktree test app can be started against the chosen environment.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 1 UAT and record results</name>
  <action>Apply approved migrations to the approved environment, run the Phase 1 UAT checklist, and record pass/fail/blocker results in docs/PHASE1_UAT_RESULTS.md.</action>
  <acceptance_criteria>
    - UAT covers opportunity CRUD, import, visibility separation, source/contact, and documents.
    - Any issue is classified as blocker, fix-before-merge, or follow-up.
    - No Phase 2 work begins until blockers are resolved.
  </acceptance_criteria>
  <done>Phase 1 is either approved for PR/merge or has a clear fix list.</done>
</task>

</tasks>

<verification>
- [ ] `git status --short` shows no committed secrets.
- [ ] Worktree app starts on test port.
- [ ] UAT result log exists.
- [ ] Release decision is documented.
</verification>

<success_criteria>
- Environment choice is explicit.
- Backup/rollback path is explicit.
- UAT is written and ready to run.
- Push/PR/merge/release responsibilities are understandable to a non-technical product owner.
</success_criteria>
