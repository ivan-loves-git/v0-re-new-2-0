---
phase: 01.1-testing-environment-and-release-protocol
plan: 02
type: execute
wave: 1
depends_on: [01.1-01]
files_modified:
  - docs/PHASE1_UAT_RESULTS.md
autonomous: false
requirements: [ENV-01, ENV-02, TEST-01]
user_setup:
  - Ivan approved using the same/current Supabase project on 2026-05-17.
  - Codex needs a connection path before applying SQL: Supabase connector, SQL editor access, or DATABASE_URL/database password.
must_haves:
  truths:
    - The target database is the approved current Re-New Supabase project.
    - This is not an isolated sandbox.
    - .env.local is local-only and must not be committed.
    - Phase 1 migrations must be additive-only.
    - UAT data must be clearly marked and cleanable.
  artifacts:
    - docs/PHASE1_UAT_RESULTS.md
  local_only:
    - .env.local
---

<objective>
Set up the Phase 1 test run against the approved current Supabase project.

Purpose: Prove the opportunity foundation works while avoiding the cost of another Supabase project.
Output: Target project confirmed, backup/rollback route recorded, additive migrations applied, local app running on port 3011, and UAT ready to start.
</objective>

<tasks>

<task type="checkpoint:setup">
  <name>Task 1: Confirm approved Supabase project and backup route</name>
  <action>Record the non-secret target Supabase URL/ref in docs/PHASE1_UAT_RESULTS.md. Confirm the backup route before migration: dashboard backup, manual logical dump, or explicitly accepted risk if neither is available.</action>
  <acceptance_criteria>
    - Ivan's same-project approval is recorded.
    - Target project URL/ref is recorded without secrets.
    - Backup/rollback route is recorded before migration.
  </acceptance_criteria>
  <done>Target approved Supabase project and backup route are known.</done>
</task>

<task type="auto">
  <name>Task 2: Configure the worktree environment</name>
  <action>Create or update the worktree .env.local from .env.test.local.example using the approved Supabase credentials. Do not commit .env.local.</action>
  <acceptance_criteria>
    - NEXT_PUBLIC_SUPABASE_URL points to the approved project.
    - DATABASE_URL, if available, points to the approved project database.
    - BETTER_AUTH_URL is http://localhost:3011.
    - No secret file is tracked by git.
  </acceptance_criteria>
  <done>Worktree is configured for the approved project.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Phase 1 additive migrations</name>
  <action>Apply scripts/044_create_opportunities_foundation.sql and scripts/045_setup_opportunity_documents_storage.sql to the approved Supabase project only after confirming backup/rollback.</action>
  <acceptance_criteria>
    - The two scripts are reviewed as additive before execution.
    - Opportunity tables exist in the approved project.
    - Opportunity document storage exists in the approved project.
    - No destructive SQL is run.
  </acceptance_criteria>
  <done>Approved database supports Phase 1 opportunity features.</done>
</task>

<task type="auto">
  <name>Task 4: Start the local test app</name>
  <action>Run the worktree app on port 3011 against the approved Supabase project and verify /opportunities loads.</action>
  <acceptance_criteria>
    - http://localhost:3011/opportunities loads from the worktree.
    - Basic database calls succeed against the approved project.
    - Any setup error is logged in docs/PHASE1_UAT_RESULTS.md.
  </acceptance_criteria>
  <done>Phase 1 UAT can begin.</done>
</task>

</tasks>

<verification>
- [ ] Approved current Supabase project is confirmed.
- [ ] Backup/rollback route is recorded.
- [ ] Worktree .env.local exists locally and is untracked.
- [ ] Phase 1 migrations applied to approved project.
- [ ] http://localhost:3011/opportunities loads.
- [ ] docs/PHASE1_UAT_RESULTS.md records environment setup status.
</verification>

<success_criteria>
- The app runs against the approved current Supabase project.
- Database and storage behavior can be tested with controlled risk.
- Ivan can test the feature without paying for another Supabase project.
</success_criteria>
