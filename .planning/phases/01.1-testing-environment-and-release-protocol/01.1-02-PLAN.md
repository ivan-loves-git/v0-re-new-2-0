---
phase: 01.1-testing-environment-and-release-protocol
plan: 02
type: execute
wave: 1
depends_on: [01.1-01]
files_modified:
  - docs/PHASE1_UAT_RESULTS.md
autonomous: false
requirements: [ENV-01, ENV-02, TEST-01]
user_setup:
  - Ivan must provide or approve the test Supabase project URL, anon key, service role key, and database password.
must_haves:
  truths:
    - The target database is the separate Supabase test project only.
    - .env.local is local-only and must not be committed.
    - Phase 1 migrations are tested before any production/current database migration is considered.
  artifacts:
    - docs/PHASE1_UAT_RESULTS.md
  local_only:
    - .env.local
---

<objective>
Set up the Phase 1 test run against the separate Supabase test project.

Purpose: Prove the opportunity foundation works against a disposable database before Phase 2 starts.
Output: Test project configured, migrations applied, local app running on port 3011, and UAT ready to start.
</objective>

<tasks>

<task type="checkpoint:setup">
  <name>Task 1: Create or identify the test Supabase project</name>
  <action>Use an existing empty test Supabase project or create a new one named clearly as Re-New test/staging. Record only non-secret identifiers in docs/PHASE1_UAT_RESULTS.md.</action>
  <acceptance_criteria>
    - The project is separate from the current/shared Re-New Supabase database.
    - The project purpose is clearly test/staging.
    - The production/current project is not used.
  </acceptance_criteria>
  <done>Target test Supabase project is known.</done>
</task>

<task type="auto">
  <name>Task 2: Configure the worktree environment</name>
  <action>Create or update the worktree .env.local from .env.test.local.example using the test Supabase credentials. Do not commit .env.local.</action>
  <acceptance_criteria>
    - NEXT_PUBLIC_SUPABASE_URL points to the test project.
    - DATABASE_URL points to the test project database.
    - BETTER_AUTH_URL is http://localhost:3011.
    - No secret file is tracked by git.
  </acceptance_criteria>
  <done>Worktree is configured for the test project.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Phase 1 migrations to the test project</name>
  <action>Apply scripts/044_create_opportunities_foundation.sql and scripts/045_setup_opportunity_documents_storage.sql to the test Supabase project only.</action>
  <acceptance_criteria>
    - Opportunity tables exist in the test project.
    - Opportunity document storage exists in the test project.
    - No migration is applied to the current/shared project.
  </acceptance_criteria>
  <done>Test database supports Phase 1 opportunity features.</done>
</task>

<task type="auto">
  <name>Task 4: Start the local test app</name>
  <action>Run the worktree app on port 3011 against the test Supabase project and verify /opportunities loads.</action>
  <acceptance_criteria>
    - http://localhost:3011/opportunities loads from the worktree.
    - Basic database calls succeed against the test project.
    - Any setup error is logged in docs/PHASE1_UAT_RESULTS.md.
  </acceptance_criteria>
  <done>Phase 1 UAT can begin.</done>
</task>

</tasks>

<verification>
- [ ] Test Supabase project is separate from current/shared Supabase.
- [ ] Worktree .env.local exists locally and is untracked.
- [ ] Phase 1 migrations applied to test project.
- [ ] http://localhost:3011/opportunities loads.
- [ ] docs/PHASE1_UAT_RESULTS.md records environment setup status.
</verification>

<success_criteria>
- The app runs against fake/disposable data.
- Database and storage behavior can be tested safely.
- Ivan can test the feature without risking real Re-New data.
</success_criteria>
